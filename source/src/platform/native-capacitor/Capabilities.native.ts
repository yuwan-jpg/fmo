import type { ICapabilities } from '../interfaces/ICapabilities'

export const nativeCapabilities: ICapabilities = {
  hasNativeAprs: true,
  hasBackgroundMode: true,
  hasPersistentGridCache: true,
  hasNativeEvents: true,
  hasNativeAudio: true,
  hasNativeLocation: true
}
